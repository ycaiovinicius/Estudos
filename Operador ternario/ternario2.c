#include <stdio.h>

int main() {
int temperatura = 30;


temperatura > 30 ? printf("A temperatura está alta\n") : printf("A temperatura está baixa\n");


/*
resultado = (temperatura >= 30) ? 1 : 0;

    if (resultado == 1) {
        printf("Temperatura alta\n");
    } else {
        printf("Temperatura baixa\n");
   }
*/

    return 0;
}