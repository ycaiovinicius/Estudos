#include <stdio.h>

int main(){


    int numeroX, numeroY;
    //Operadores aritmeticos
    int soma = numeroX + numeroY;
    int subtracao = numeroX - numeroY;
    int multiplicacao = numeroX * numeroY;
    int divisao = numeroX / numeroY;
    
    
    
    //entrada de dados
    printf("SEJA BEM VINDO A CALCULADORA!\n");
    printf("Insira abaixo numeros inteiros na qual você quer calcular:");
scanf("%d, &numeroX");
scanf ("%d, &numeroY");